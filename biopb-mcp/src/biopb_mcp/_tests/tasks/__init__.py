"""Functional task benchmark: can an agent do a named piece of work against
real data, in a real session. See README.md.

Skill-independent by construction -- there is no ablation, one arm, and N
samples. The machinery is shared with `skills/interaction` and lives in
`agentbench`; what is here is the one-arm loop and the cases.
"""
