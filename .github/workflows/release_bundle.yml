name: Build Bundled Installer

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to build from (branch, tag, or commit)"
        required: false
        default: ""

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
          - os: windows-latest
            platform: windows
          - os: macos-15
            platform: macos-intel
          - os: macos-14
            platform: macos-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.ref || '' }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip cache purge
          pip install pyinstaller
          pip install napari[all]
          pip install .

      - name: Setup headless display (Linux)
        if: runner.os == 'Linux'
        uses: pyvista/setup-headless-display-action@v4
        with:
          qt: true

      - name: Build with PyInstaller
        run: pyinstaller --noconfirm --clean napari-biopb.spec

      - name: Create archive (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          cd dist
          if [ -d "napari-biopb.app" ]; then
            tar -czvf napari-biopb-${{ matrix.platform }}.tar.gz napari-biopb.app
          else
            tar -czvf napari-biopb-${{ matrix.platform }}.tar.gz napari-biopb
          fi

      - name: Create archive (Windows)
        if: runner.os == 'Windows'
        run: |
          cd dist
          7z a napari-biopb-${{ matrix.platform }}.zip napari-biopb

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: napari-biopb-${{ matrix.platform }}
          path: dist/napari-biopb-${{ matrix.platform }}.*
          retention-days: 7

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          pattern: napari-biopb-*
          merge-multiple: true

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: napari-biopb ${{ steps.version.outputs.VERSION }}
          body: |
            ## Installation

            Download the appropriate installer for your platform:

            - **Windows**: Download `napari-biopb-windows.zip`, extract, and run `napari-biopb.exe`
            - **macOS (Intel)**: Download `napari-biopb-macos-intel.tar.gz`, extract `napari-biopb.app`, and open it
            - **macOS (Apple Silicon)**: Download `napari-biopb-macos-arm.tar.gz`, extract `napari-biopb.app`, and open it
            - **Linux**: Download `napari-biopb-linux.tar.gz`, extract, and run `./napari-biopb`

            **Note**: These are unsigned installers. On macOS, right-click and select "Open" to bypass Gatekeeper. On Windows, click "Run anyway" when prompted.

            This bundle includes napari with the napari-biopb plugin pre-installed.
          files: artifacts/*
          draft: false
          prerelease: false
