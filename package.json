{
  "name": "biopb-workspace",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "build": "pnpm -r run build",
    "dev:web": "pnpm --filter @biopb/web dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "buf:generate": "buf generate",
    "sync-version": "node biopb-tensor-server/scripts/sync-version.js"
  },
  "dependencies": {
    "@bufbuild/buf": "^1.66.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
