{
  "name": "biopb-workspace",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "build": "pnpm -C web run build",
    "dev:web": "pnpm -C web run dev",
    "test": "pnpm -C web run test",
    "lint": "pnpm -C web run lint",
    "buf:generate": "buf generate",
    "sync-version": "pnpm -C web run sync-version"
  },
  "dependencies": {
    "@bufbuild/buf": "^1.66.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
